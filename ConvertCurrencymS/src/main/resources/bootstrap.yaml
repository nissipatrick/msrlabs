spring:
  cloud:
    config:
      uri: ${convertcurrencyms.config.server.url:http://localhost:8888}
      fail-fast: true
      
  application:
    name: convertcurrencyms
  profiles:
    active: dev